# Gas Town - Повний посібник користувача

**Версія:** 0.4.0
**Мова документа:** Українська

---

## Зміст

1. [Що таке Gas Town?](#що-таке-gas-town)
2. [Основні поняття](#основні-поняття)
3. [Встановлення та налаштування](#встановлення-та-налаштування)
4. [Структура Town](#структура-town)
5. [Команди gt](#команди-gt)
6. [Агенти та їх ролі](#агенти-та-їх-ролі)
7. [Робота з Beads (задачами)](#робота-з-beads-задачами)
8. [Convoy — групи задач](#convoy--групи-задач)
9. [Формули та Молекули](#формули-та-молекули)
10. [Система пошти між агентами](#система-пошти-між-агентами)
11. [Hook — система збереження прогресу](#hook--система-збереження-прогресу)
12. [Робочі процеси](#робочі-процеси)
13. [Розширені можливості](#розширені-можливості)
14. [Усунення проблем](#усунення-проблем)
15. [Глосарій](#глосарій)

---

## Що таке Gas Town?

Gas Town — це система оркестрації AI-агентів для Claude Code. Уяви, що замість одного помічника у тебе є **ціла команда AI-ботів**, кожен з яких:

- Памʼятає свою роботу навіть після перезапуску
- Може передавати роботу іншому агенту
- Працює над окремою задачею паралельно з іншими

### Яку проблему вирішує Gas Town?

| Проблема | Рішення Gas Town |
|----------|------------------|
| Агенти втрачають контекст після перезапуску | Робота зберігається в git-backed hooks |
| Ручна координація агентів | Вбудована пошта, ідентичності, handoffs |
| 4-10 агентів = хаос | Комфортно масштабується до 20-30 агентів |
| Стан роботи губиться в памʼяті агента | Стан зберігається в Beads ledger |

### Аналогія для розуміння

Уяви **будівельну компанію**:

- **Town (~/gt/)** = Головний офіс компанії
- **Mayor** = Директор, який розподіляє проєкти
- **Rig** = Будівельний майданчик (один проєкт)
- **Polecat** = Робітник, який приходить, робить задачу і йде
- **Crew** = Постійний працівник на проєкті
- **Hook** = Дошка з поточними завданнями
- **Bead** = Картка із завданням
- **Convoy** = Група повʼязаних завдань

---

## Основні поняття

### Town 🏘️

Твоя головна робоча директорія (зазвичай `~/gt/`). Містить всі проєкти, агентів та конфігурацію.

### Rig 🏗️

Контейнер для одного проєкту. Кожен rig:
- Обгортає git-репозиторій
- Має власних агентів
- Має власний префікс для задач (наприклад, `gt-`, `bd-`)

### Агенти 🤖

AI-помічники різних типів:
- **Mayor** — головний координатор
- **Polecat** — тимчасовий робітник
- **Crew** — постійний працівник
- **Witness** — спостерігач за polecats
- **Refinery** — обробляє merge queue
- **Deacon** — фоновий патруль

### Bead 📿

Одиниця роботи (задача, issue). Зберігається в git як JSONL.

Формат ID: `<prefix>-<5 символів>`
- `gt-abc12` — задача в rig "gastown"
- `hq-xyz99` — задача на рівні town

### Hook 🪝

"Гачок" — місце, куди вішається поточна робота агента. Якщо щось на хуку — агент має це виконати.

### Convoy 🚚

Група повʼязаних задач. Як "квестова лінія" в грі — кілька задач обʼєднані спільною метою.

### Molecule 🧬

Багатокроковий робочий процес з persistentним станом. Кожен крок — окрема задача.

---

## Встановлення та налаштування

### Вимоги

| Компонент | Мінімальна версія |
|-----------|-------------------|
| Go | 1.23+ |
| Git | 2.25+ (для worktrees) |
| beads (bd) | 0.44.0+ |
| sqlite3 | будь-яка |
| tmux | 3.0+ (рекомендовано) |
| Claude Code CLI | остання |

### Встановлення

```bash
# Варіант 1: З вихідників
git clone https://github.com/joyshmitz/gastown.git
cd gastown
make build
sudo mv gt /usr/local/bin/

# Варіант 2: go install
go install github.com/steveyegge/gastown/cmd/gt@latest

# Варіант 3: npm (якщо є релізи)
npm install -g @gastown/gt
```

### Перша ініціалізація

```bash
# Створити town з git
gt install ~/gt --git

# Перейти в town
cd ~/gt

# Перевірити налаштування
gt doctor

# Якщо є проблеми — автовиправлення
gt doctor --fix
```

### Додати перший проєкт

```bash
# Додати rig (проєкт)
gt rig add myproject https://github.com/you/repo.git

# Перевірити
gt rig list

# Створити crew workspace (для себе)
gt crew add yourname --rig myproject
```

---

## Структура Town

```
~/gt/                              ← Town root
├── .beads/                        ← Town-level beads (hq-* prefix)
│   ├── issues.jsonl               ← Задачі
│   ├── routes.jsonl               ← Маршрути beads
│   └── formulas/                  ← Формули workflows
│
├── mayor/                         ← Офіс Mayor
│   ├── town.json                  ← Конфігурація town
│   ├── rigs.json                  ← Список rigs
│   ├── CLAUDE.md                  ← Контекст для Mayor
│   └── .claude/settings.json      ← Налаштування Claude
│
├── deacon/                        ← Daemon beacon
│   ├── heartbeat.json             ← Health check файл
│   └── dogs/boot/                 ← Boot triage dog
│
├── settings/                      ← Глобальні налаштування
│   ├── config.json                ← Конфіг агентів
│   └── escalation.json            ← Правила ескалації
│
└── <rig>/                         ← Контейнер проєкту
    ├── config.json                ← Rig identity
    ├── .repo.git/                 ← Bare repo (shared by worktrees)
    ├── mayor/rig/                 ← Mayor's clone
    ├── refinery/rig/              ← Worktree on main
    ├── witness/                   ← Monitor-only
    ├── crew/<name>/rig/           ← Human workspaces
    └── polecats/<name>/rig/       ← Worker worktrees
```

### Конфігураційні файли

**Rig config (config.json):**
```json
{
  "type": "rig",
  "name": "myproject",
  "git_url": "https://github.com/...",
  "beads": { "prefix": "mp" }
}
```

**Settings (settings/config.json):**
```json
{
  "type": "rig-settings",
  "version": 1,
  "agent": "claude",
  "agents": {
    "opencode": {
      "command": "opencode",
      "args": ["--session"]
    }
  }
}
```

**Escalation (settings/escalation.json):**
```json
{
  "type": "escalation",
  "version": 1,
  "routes": {
    "low": ["bead"],
    "medium": ["bead", "mail:mayor"],
    "high": ["bead", "mail:mayor", "email:human"],
    "critical": ["bead", "mail:mayor", "email:human", "sms:human"]
  }
}
```

---

## Команди gt

### Налаштування та діагностика

```bash
gt install [path]              # Створити town workspace
gt install --git               # З git ініціалізацією
gt doctor                      # Перевірка здоровʼя
gt doctor --fix                # Автовиправлення проблем
gt status                      # Загальний статус
gt info                        # Інформація про версію
gt info --whats-new            # Що нового
```

### Управління Rigs (проєктами)

```bash
gt rig add <name> <url>        # Додати проєкт
gt rig list                    # Список проєктів
gt rig remove <name>           # Видалити проєкт
```

**Приклади:**
```bash
# Додати GitHub репозиторій
gt rig add myapp https://github.com/me/myapp.git

# Додати локальний проєкт
gt rig add localproj ~/projects/localproj
```

### Управління Crew (постійні працівники)

```bash
gt crew add <name> --rig <rig>   # Створити crew workspace
gt crew list                      # Список crew members
gt crew at <name>                 # Працювати як crew member
```

**Приклад:**
```bash
# Створити workspace для себе
gt crew add serhii --rig myapp

# Перейти в workspace
cd ~/gt/myapp/crew/serhii/rig
```

### Операції з агентами

```bash
gt agents                        # Список активних агентів
gt peek <agent>                  # Перевірити стан агента
gt nudge <agent> "message"       # Надіслати повідомлення агенту
gt stop --all                    # Зупинити всі сесії
gt stop --rig <name>             # Зупинити сесії rig
```

### Mayor (головний координатор)

```bash
gt mayor attach                  # Запустити сесію Mayor
gt mayor start --agent <alias>   # Mayor з конкретним агентом
```

**Що робить Mayor:**
1. Аналізує твої запити
2. Створює convoy з задачами
3. Розподіляє роботу між агентами
4. Слідкує за прогресом

### Convoy (групи задач)

```bash
gt convoy create "name" [issues...]   # Створити convoy
gt convoy list                        # Дашборд активних convoys
gt convoy list --all                  # Включно з завершеними
gt convoy status [id]                 # Детальний статус
gt convoy show [id]                   # Показати деталі
gt convoy add <convoy-id> <issue>     # Додати задачу в convoy
```

**Приклад:**
```bash
# Створити convoy для нової фічі
gt convoy create "User Auth" gt-abc12 gt-def34 gt-xyz99

# Перевірити статус
gt convoy list

# Детальна інформація
gt convoy show 1
```

### Sling (призначення роботи)

```bash
gt sling <bead-id> <rig>                    # Призначити задачу
gt sling <bead-id> <rig> --agent codex      # З вказаним runtime
gt sling <proto> --on <issue> <rig>         # З workflow template
```

**Приклад:**
```bash
# Призначити задачу gt-abc12 проєкту myapp
gt sling gt-abc12 myapp

# Використати Codex замість Claude
gt sling gt-abc12 myapp --agent codex
```

### Сесії та контекст

```bash
gt handoff                       # Передати роботу новій сесії
gt handoff --shutdown            # Завершити (для polecats)
gt handoff -s "Title" -m "Notes" # З нотатками для наступника
gt session stop <rig>/<agent>    # Зупинити конкретну сесію
gt seance                        # Список попередніх сесій
gt seance --talk <id> -p "?"     # Запитати попередню сесію
```

### Пошта

```bash
gt mail inbox                    # Перевірити повідомлення
gt mail read <id>                # Прочитати повідомлення
gt mail send <addr> -s "Subj" -m "Body"  # Надіслати
gt mail send --human -s "..."    # Надіслати людині (overseer)
gt mail check --inject           # Перевірити та inject в сесію
```

### Конфігурація

```bash
gt config agent list             # Список агентів
gt config agent set <name> <cmd> # Створити агента
gt config agent remove <name>    # Видалити агента
gt config default-agent [name]   # Get/set default агента
gt config show                   # Показати всю конфігурацію
```

### Ескалація

```bash
gt escalate -s <level> --subject="..." --body="..."
gt escalate ack <bead-id> --note="..."    # Acknowledge
gt escalate list --unacked                 # Непідтверджені
gt escalate close <bead-id> --reason="..." # Закрити
```

Рівні: `low`, `medium`, `high`, `critical`

### Dashboard

```bash
gt dashboard --port 8080         # Запустити веб-панель
gt dashboard --open              # Відкрити браузер автоматично
```

---

## Агенти та їх ролі

### Town-Level агенти (працюють на рівні всього town)

#### Mayor 🎩

**Хто:** Головний координатор, твій основний інтерфейс.

**Де живе:** `~/gt/mayor/`

**Що робить:**
- Приймає запити від тебе
- Створює convoy з задачами
- Розподіляє роботу між агентами
- Слідкує за загальним прогресом

**Як запустити:**
```bash
gt mayor attach
```

#### Deacon 🕯️

**Хто:** Фоновий daemon, що патрулює систему.

**Де живе:** `~/gt/deacon/`

**Що робить:**
- Моніторить здоровʼя системи
- Перевіряє heartbeat кожні 3 хвилини
- Запускає Boot для тріажу
- Будить агентів якщо потрібно

**Як працює:**
```
Daemon (Go process)
    ↓ кожні 3 хв
Boot (AI triage)
    ↓ якщо потрібно
Deacon (повний патруль)
```

### Rig-Level агенти (працюють в межах одного проєкту)

#### Polecat 🦨

**Хто:** Тимчасовий робітник для однієї задачі.

**Де живе:** `~/gt/<rig>/polecats/<name>/rig/`

**Що робить:**
- Spawns для конкретної задачі
- Працює в ізольованому git worktree
- Завершує роботу і "вмирає"

**Життєвий цикл:**
```
gt sling → spawn → робота → gt done → cleanup
```

**Стани:**
- **Working** — активно працює
- **Stalled** — сесія зупинилась (crashed)
- **Zombie** — завершив роботу, але не закрився

#### Witness 👁️

**Хто:** Спостерігач за polecats.

**Що робить:**
- Моніторить здоровʼя polecats
- Перезапускає застряглих
- Координує cleanup після завершення

#### Refinery ⚙️

**Хто:** Обробник merge queue.

**Де живе:** `~/gt/<rig>/refinery/rig/`

**Що робить:**
- Приймає готові branches
- Робить code review
- Мерджить в main
- Обробляє конфлікти

#### Crew 👤

**Хто:** Постійний працівник (зазвичай ти).

**Де живе:** `~/gt/<rig>/crew/<name>/rig/`

**Що робить:**
- Тривала робота над проєктом
- Прямий доступ до коду
- Ручне управління задачами

### Змінні середовища за роллю

| Роль | Змінні |
|------|--------|
| Mayor | `GT_ROLE=mayor`, `BD_ACTOR=mayor` |
| Deacon | `GT_ROLE=deacon`, `BD_ACTOR=deacon` |
| Witness | `GT_ROLE=witness`, `GT_RIG=<rig>` |
| Refinery | `GT_ROLE=refinery`, `GT_RIG=<rig>` |
| Polecat | `GT_ROLE=polecat`, `GT_RIG=<rig>`, `GT_POLECAT=<name>` |
| Crew | `GT_ROLE=crew`, `GT_RIG=<rig>`, `GT_CREW=<name>` |

---

## Робота з Beads (задачами)

Beads — це git-backed система трекінгу задач. Кожна задача = bead.

### Основні команди bd

```bash
# Створити задачу
bd new "Назва задачі"
bd create --title="..." --type=task

# Список задач
bd list
bd list --status=open
bd list --parent=<id>           # Дочірні задачі

# Показати деталі
bd show <id>

# Оновити статус
bd update <id> --status=in_progress
bd update <id> --status=closed

# Закрити з авто-просуванням
bd close <id> --continue

# Синхронізувати з git
bd sync
```

### Формат ID

`<prefix>-<5 символів>`

- `hq-*` — town-level beads (в `~/gt/.beads/`)
- `<rig>-*` — rig-level beads (в `~/gt/<rig>/mayor/rig/.beads/`)

**Приклади:**
```
gt-abc12    ← задача в rig "gastown"
bd-xyz99    ← задача в rig "beads"
hq-cv-001   ← convoy на рівні town
```

### Routing

Beads автоматично маршрутизуються:

```
gt-abc12 → ~/gt/gastown/mayor/rig/.beads/
bd-xyz99 → ~/gt/beads/mayor/rig/.beads/
hq-abc12 → ~/gt/.beads/
```

Маршрути визначені в `~/gt/.beads/routes.jsonl`

---

## Convoy — групи задач

Convoy — це група повʼязаних задач, обʼєднаних спільною метою.

### Створення convoy

```bash
# Базове створення
gt convoy create "Feature Name" gt-abc12 gt-def34 gt-xyz99

# З нотифікацією
gt convoy create "Feature Name" gt-abc12 --notify mayor/

# З нотифікацією людини
gt convoy create "Feature Name" gt-abc12 --notify --human
```

### Перегляд convoy

```bash
# Дашборд
gt convoy list

# Всі (включно з завершеними)
gt convoy list --all

# Тільки завершені
gt convoy list --status=closed

# Деталі конкретного
gt convoy show 1
gt convoy status hq-cv-abc
```

### Життєвий цикл

```
OPEN ──(всі issues закриті)──► LANDED/CLOSED
  ↑                                │
  └──(додати ще issues)────────────┘
       (авто-відкривається)
```

### Auto-Convoy

Якщо sling одну задачу без convoy — створюється автоматичний:

```bash
gt sling bd-xyz beads/amber
# → Створює: "Work: bd-xyz"
```

### Cross-Rig tracking

Convoy може трекати задачі з різних rigs:

```bash
gt convoy create "Multi-project" gt-abc12 bd-xyz99 hq-todo55
```

---

## Формули та Молекули

### Що таке формула?

Формула — це **шаблон робочого процесу** (workflow template). Записується в TOML.

### Що таке молекула?

Молекула — це **екземпляр формули** з конкретними даними та станом.

### Життєвий цикл

```
Formula (TOML шаблон)
    ↓ bd cook
Protomolecule (заморожений шаблон)
    ├─→ bd mol pour → Molecule (persistent)
    └─→ bd mol wisp → Wisp (ephemeral)
```

### Типи формул

#### 1. Workflow — послідовні кроки

```toml
formula = "release"
type = "workflow"

[vars.version]
description = "Version to release"
required = true

[[steps]]
id = "test"
title = "Run Tests"

[[steps]]
id = "build"
title = "Build"
needs = ["test"]

[[steps]]
id = "publish"
title = "Publish"
needs = ["build"]
```

#### 2. Convoy — паралельні legs

```toml
formula = "security-scan"
type = "convoy"

[[legs]]
id = "sast"
title = "Static Analysis"

[[legs]]
id = "deps"
title = "Dependency Audit"

[synthesis]
title = "Security Report"
depends_on = ["sast", "deps"]
```

#### 3. Expansion — параметризовані шаблони

```toml
formula = "component-review"
type = "expansion"

[[template]]
id = "analyze"
title = "Analyze {{component}}"

[[template]]
id = "test"
title = "Test {{component}}"
needs = ["analyze"]
```

#### 4. Aspect — багатоаспектний аналіз

```toml
formula = "code-review"
type = "aspect"

[[aspects]]
id = "security"
title = "Security Review"

[[aspects]]
id = "performance"
title = "Performance Review"
```

### Команди для молекул

```bash
# Список формул
bd formula list
bd formula show <name>

# Приготувати формулу → protomolecule
bd cook <formula>

# Список protomolecules
bd mol list

# Створити molecule
bd mol pour <proto>          # Persistent
bd mol wisp <proto>          # Ephemeral

# Статус
bd mol show <id>
bd mol current               # Де я зараз?
bd mol progress <id>

# Приєднати до bead
bd mol attach <bead> <mol>
bd mol detach <bead>

# Навігація
bd ready                     # Наступний ready крок
bd close <step> --continue   # Закрити і авто-просунути
```

### Best practices

1. **Закривай кроки в реальному часі** — mark in_progress перед, closed одразу після
2. **Використовуй --continue** — автоматично просуває до наступного кроку
3. **Не читай формули напряму** — працюй з cooked molecules
4. **Молекули = ledger** — кожне закриття кроку = timestamped запис в CV

---

## Система пошти між агентами

Агенти спілкуються через внутрішню пошту.

### Типи повідомлень

| Тип | Маршрут | Призначення |
|-----|---------|-------------|
| POLECAT_DONE | Polecat → Witness | Сигнал завершення роботи |
| MERGE_READY | Witness → Refinery | Branch готовий до merge |
| MERGED | Refinery → Witness | Підтвердження merge |
| MERGE_FAILED | Refinery → Witness | Помилка merge |
| REWORK_REQUEST | Refinery → Witness | Запит на rebase |
| HELP | Any → Escalation | Запит допомоги |
| HANDOFF | Agent → successor | Передача сесії |

### Формат адреси

`<rig>/<role>` або `<rig>/<type>/<name>`

**Приклади:**
```
myapp/witness           ← Witness проєкту myapp
myapp/polecats/alpha    ← Polecat "alpha" в myapp
mayor/                  ← Mayor
deacon/                 ← Deacon
```

### Команди пошти

```bash
# Вхідні
gt mail inbox
gt mail read <id>

# Надіслати
gt mail send myapp/witness -s "Subject" -m "Body"
gt mail send --human -s "Help needed"

# Канали
gt mail channel subscribe <channel>
gt mail channel unsubscribe <channel>

# Ідентичності
gt mail identity list
gt mail identity set <name> <value>
```

### Формат повідомлення

**Subject:** TYPE: brief context
```
POLECAT_DONE: alpha
MERGE_READY: myapp/alpha
HELP: Stuck on tests
🤝 HANDOFF: Schema work
```

**Body:** Key-value pairs + markdown
```
branch: feature/login
status: ready
commit: abc123

## Details
Work completed successfully.
```

---

## Hook — система збереження прогресу

### Що таке Hook?

Hook — це "гачок", на який вішається поточна робота агента. Якщо на хуку є робота — агент **МУСИТЬ** її виконати.

### GUPP — Gas Town Universal Propulsion Principle

> "Якщо щось на твоєму хуку — ТИ ЦЕ ВИКОНУЄШ"

Це основний принцип Gas Town:
- Ніякого polling від supervisor
- Hook = твоє завдання
- Вся пропускна здатність системи залежить від негайного виконання
- Інші можуть чекати на твій output

### Команди для hook

```bash
# Що на моєму хуку?
gt hook

# Приєднати роботу з пошти
gt mol attach-from-mail <id>

# Орієнтація
bd mol current               # Де я в молекулі?
bd ready                     # Який крок наступний?
bd show <step-id>            # Що вимагає цей крок?
```

### Propulsion loop (цикл просування)

```bash
1. gt hook                    # Що на хуку?
2. bd mol current             # Де я?
3. Виконати крок
4. bd close <step> --continue # Закрити і просунути
5. GOTO 2
```

### Persistence через git worktrees

- Worktrees переживають цикли сесій та перезапуски
- Commit state, branch, staged changes — все зберігається
- Нова сесія продовжує з того місця через `gt prime`

---

## Робочі процеси

### 1. Mayor-led workflow (рекомендований)

**Для:** Координація складної, багатозадачної роботи.

```bash
# 1. Запустити Mayor
gt mayor attach

# 2. В сесії Mayor — створити convoy
gt convoy create "Feature X" gt-abc12 gt-def34 --notify

# 3. Mayor аналізує та spawns агентів за потребою

# 4. Моніторинг
gt convoy list

# 5. Результати коли convoy landed
```

### 2. Manual workflow

**Для:** Повний контроль над процесом.

```bash
# 1. Створити convoy
gt convoy create "Bug Fixes" --human

# 2. Призначити роботу
gt sling gt-abc12 myproject

# 3. Перевірити прогрес
gt convoy show

# 4. Обробити завершення через mail
gt mail inbox
```

### 3. Minimal mode (без tmux)

**Для:** Прості задачі, lightweight процес.

```bash
gt convoy create "Fix bug" gt-abc12
gt sling gt-abc12 myproject
claude --resume               # Агент читає mail, виконує
gt convoy list                # Перевірка
```

### 4. Crew member workflow

**Для:** Постійна робота над проєктом.

```bash
cd ~/gt/myproject/crew/yourname/rig

# Працювати напряму
git add && git commit -m "Fix..."
git push

# Оновити задачу
bd update gt-abc12 --status=closed
```

### 5. Cross-rig workflow

**Для:** Робота з кількома проєктами одночасно.

```bash
# З gastown rig, працювати над beads issue
gt worktree beads
cd ~/gt/beads/crew/gastown-yourname/

# Працювати як: gastown/crew/yourname (attribution preserved)
```

---

## Розширені можливості

### Watchdog Chain (Daemon/Boot/Deacon)

**Архітектура:**
```
Daemon (Go process)
    ↓ heartbeat кожні 3 хв
Boot (AI triage agent)
    ↓ рішення
Deacon (повний патруль)
```

**Boot decision matrix:**

| Вік | Стан | Дія |
|-----|------|-----|
| < 5 хв | Fresh | Нічого |
| 5-15 хв | Stale | Nudge якщо pending mail |
| > 15 хв | Very stale | Wake Deacon |

**Heartbeat файл:** `~/gt/deacon/heartbeat.json`

### Polecat lifecycle (три рівні)

1. **Session** — Claude instance (ephemeral, cycles per step/handoff)
2. **Sandbox** — Git worktree (persistent until nuke)
3. **Slot** — Імʼя з pool (persistent until nuke)

**Self-cleaning model:**
```
1. gt sling → spawn (allocate slot, create sandbox, start session)
2. Робота (session cycles via handoff)
3. gt done → cleanup (push, submit to MQ, request nuke, exit)
4. Refinery: merge queue, handle conflicts
5. Witness: nuke after merge confirmed
```

### Plugin system

Plugins запускаються як patrol molecules:

```json
{
  "id": "mol-security-scan",
  "labels": ["template", "plugin", "witness", "tier:haiku"]
}
```

Bonding до patrol:
```bash
bd mol bond mol-security-scan $PATROL_ID --var scope="$SCOPE"
```

### Seance — розмова з попередниками

```bash
# Список попередніх сесій
gt seance

# Запитати попередню сесію
gt seance --talk <id> -p "Що ти робив з auth модулем?"
```

### Sparse checkout

Gas Town автоматично налаштовує sparse checkout щоб виключити:
- `.claude/` — settings, rules, agents
- `CLAUDE.md` — primary context
- `CLAUDE.local.md` — personal context
- `.mcp.json` — MCP configuration

Це гарантує, що агенти використовують контекст Gas Town, а не source repo.

---

## Усунення проблем

### gt doctor

```bash
# Перевірка здоровʼя
gt doctor

# Автовиправлення
gt doctor --fix
```

### Застряглий polecat

```bash
# Перевірити стан
gt peek myapp/polecats/alpha

# Nudge (підштовхнути)
gt nudge myapp/polecats/alpha "Continue your work"

# Зупинити
gt session stop myapp/polecats/alpha
```

### Zombie polecats

Zombie = завершив роботу, але не закрився.

```bash
# Witness має очистити автоматично
# Якщо ні — вручну:
gt stop --rig myapp
```

### Проблеми з beads

```bash
# Синхронізувати
bd sync

# Перевірити routes
cat ~/gt/.beads/routes.jsonl

# Перевірити конкретний bead
bd show gt-abc12
```

### Проблеми з git worktrees

```bash
# Список worktrees
git worktree list

# Видалити зламаний
git worktree remove <path>

# Prune orphaned
git worktree prune
```

### Логи та діагностика

```bash
# Activity log
gt log

# Feed (real-time)
gt feed

# Audit по актору
gt audit --actor=myapp/polecats/alpha
```

---

## Глосарій

| Термін | Опис |
|--------|------|
| **Bead** | Git-backed atomic work unit (JSONL format) |
| **Convoy** | Група повʼязаних задач |
| **Crew** | Постійний працівник в rig |
| **Deacon** | Фоновий daemon для patrol |
| **Formula** | TOML шаблон workflow |
| **GUPP** | Gas Town Universal Propulsion Principle |
| **Handoff** | Передача сесії наступнику |
| **Hook** | Місце для поточної роботи агента |
| **Mayor** | Головний координатор |
| **MEOW** | Molecular Expression of Work |
| **Molecule** | Екземпляр формули з станом |
| **NDI** | Nondeterministic Idempotence |
| **Polecat** | Тимчасовий worker agent |
| **Propulsion** | Негайне виконання hooked роботи |
| **Refinery** | Обробник merge queue |
| **Rig** | Контейнер проєкту |
| **Seance** | Запит до попередніх сесій |
| **Sling** | Призначення роботи агенту |
| **Town** | Головна робоча директорія |
| **Witness** | Спостерігач за polecats |
| **Wisp** | Ephemeral molecule |

---

## Корисні посилання

- [README](../README.md) — Основна документація
- [INSTALLING](INSTALLING.md) — Детальна інструкція встановлення
- [DEPLOY-UBUNTU](DEPLOY-UBUNTU.md) — Розгортання на Ubuntu
- [Glossary](glossary.md) — Повний глосарій термінів
- [Reference](reference.md) — Технічна довідка

---

**Автор:** @joyshmitz
**Дата:** 2026-01-20
**Версія документа:** 1.0.0
